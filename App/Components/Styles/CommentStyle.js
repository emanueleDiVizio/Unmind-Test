import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    flex: 1
  },
  input: {
    height: 200,
    backgroundColor: 'white',
    paddingTop: 8,
    paddingLeft: 8
  }
})
