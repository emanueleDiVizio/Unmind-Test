import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
  },
  input: {
    height: 200,
    backgroundColor: 'white',
    paddingTop: 8,
    paddingLeft: 8
  }
})
